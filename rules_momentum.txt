SI price > np.mean(price_history[-5:]) ENTONCES agent.holdings += agent.cash // price; agent.cash -= (agent.cash // price) * price
SI price <= np.mean(price_history[-5:]) ENTONCES agent.cash += agent.holdings * price; agent.holdings = 0
